# Handoff — 2026-04-26

Generated by `/wrap` at 22:10. Project: new-mac-setup. Read this first; the items below are what the previous session left live.

## Active plan / next action
- **Refactor complete locally; user smoke test on new mac is the live next action** → `~/.claude/plans/piped-riding-valiant.md`. Status: in-progress (Phases 1-5 done locally, Phase 5 "new-mac smoke test" pending user). Resume by reading that plan's "New-mac smoke test (separate session, on the new machine)" section.

## Verify on resume
- Cursor uses VS Code extensions natively → confirmed (cask "cursor" exists, current 3.2.11). Run `cursor --install-extension <id>` after install.
- `cask "proton-pass"` exists → confirmed via formulae.brew.sh (current 1.36.1).
- `mcfly init zsh` works as drop-in for `atuin init zsh` → still pending; verify on the new mac after editing `~/.dotfiles/zsh/.zshrc`.
- ~~`brew "rustup-init"` installs Rust correctly~~ → **formula renamed to `rustup` (2026); the doc has been updated.** Run `rustup-init -y` once installed (the binary kept its name).
- `brew "oven-sh/bun/bun"` auto-implies the tap → tap exists; the diff lists the explicit `tap "oven-sh/bun"` line as belt-and-suspenders, so this is no longer load-bearing.

## Open questions (still live)
- Whether to commit `CLAUDE.md`, `docs/`, and `.claude/session-history/`. Recommendation: commit `CLAUDE.md` (project conventions, referenced by hard rules) and `docs/` (source of truth for Brewfile decisions). Add `.claude/session-history/` to `.gitignore` (machine-local). User confirmation needed before staging.

## Resolved this session (2026-04-26 cont'd)
- Diff scan in `docs/tool-recommendations-2026-04.md` has no double-defines or name collisions across add/remove lines.
- `setup.sh` passes `bash -n` and `shellcheck` cleanly.
- `brew "shellcheck"` added to the recommendations doc (section I + diff). Supports the "shellcheck-clean is required" hard rule.

## Untested assumptions (still live)
- macOS 16 Brewfile install support for all listed casks → verify by `brew bundle check --file=~/.dotfiles/Brewfile` on the new mac before the real run.

## Stale artifacts
- `~/mac-setup.log` on Windows → harmless, leftover from local dry-run testing. Delete: `rm ~/mac-setup.log`.
- `~/.mac-setup-state` on Windows → harmless, leftover from local dry-run testing. Delete: `rm ~/.mac-setup-state`.

## Pointer
- Resume context: read `docs/tool-recommendations-2026-04.md` for the Brewfile diff to apply to `~/.dotfiles/Brewfile`.
- Project conventions: `CLAUDE.md`.
- Active plan: `~/.claude/plans/piped-riding-valiant.md`.
